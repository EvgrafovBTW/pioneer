name: pioneer_router
description: "A type-safe navigation package for Flutter with independent branch stacks."
version: 0.1.0

repository: https://github.com/EvgrafovBTW/pioneer

topics:
  - navigation
  - routing
  - deep-linking

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
